node_modules/
dist/
build/
.cache/
.env*
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/

# Local agent/tooling artifacts
/.claude/
/.figma/
/.playwright-mcp/
/.specify/
/docs/
/.mcp.json
/.mise.toml
/AGENTS.md
*mcp*/
contentlens-*.png

# Backend/config artifacts kept out of this frontend-only public repo
/supabase/
/utils/supabase/
